---
import Layout from '../layouts/Layout.astro';
---

<Layout>
	<main class="success-container">
		<section class="success-message fade-in">
			<h1 class="zen-mincho">縁が結ばれました</h1>
			<p class="meaning-text">
				蒐集庵の収蔵品の一つが、あなたの元へと旅立つ準備を始めました。<br>
				追って文（メール）にて、詳細をお知らせいたします。
			</p>
			<a href="/catalog" class="back-link">目録に戻る</a>
		</section>
	</main>
</Layout>

<style>
	.success-container {
		display: flex;
		align-items: center;
		justify-content: center;
		min-height: 80vh;
		padding: 2rem;
		text-align: center;
	}

	.success-message {
		max-width: 600px;
	}

	h1 {
		font-size: 2.5rem;
		margin-bottom: 2rem;
		color: var(--accent-primary);
	}

	.meaning-text {
		font-size: 1.1rem;
		line-height: 1.8;
		margin-bottom: 3rem;
		color: var(--text-muted);
	}

	.back-link {
		display: inline-block;
		text-decoration: none;
		color: var(--accent-primary);
		border: 1px solid var(--accent-primary);
		padding: 0.8rem 2rem;
		font-size: 0.9rem;
		letter-spacing: 0.1em;
		transition: all 0.3s;
	}

	.back-link:hover {
		background: var(--accent-primary);
		color: var(--bg-primary);
	}

	@keyframes fadeIn {
		from { opacity: 0; transform: translateY(10px); }
		to { opacity: 1; transform: translateY(0); }
	}

	.fade-in {
		animation: fadeIn 1.5s ease-out forwards;
	}
</style>
